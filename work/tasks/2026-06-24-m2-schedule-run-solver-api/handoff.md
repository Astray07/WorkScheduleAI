# M2 ScheduleRun Solver API Handoff

## 현재 상태

ShiftType/ShiftRequirement가 있는 조직의 ScheduleRun result를 OR-Tools solver로 생성하도록 연결했고 전체 검증과 커밋을 완료했습니다.

## 다음 단계

1. 다음 단계로 ScheduleRun worker 상태 전이와 cancel/retry 계약을 보강합니다.
