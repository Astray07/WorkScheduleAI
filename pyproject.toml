[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "work-schedule-ai"
version = "0.1.0"
description = "WorkScheduleAI backend scaffold and contract tests"
requires-python = ">=3.13"
dependencies = ["fastapi>=0.128,<1", "ortools>=9.14,<10", "SQLAlchemy>=2.0,<3"]

[project.optional-dependencies]
deploy = ["alembic>=1.16,<2", "psycopg[binary]>=3.2,<4", "uvicorn[standard]>=0.40,<1"]
dev = ["alembic>=1.16,<2", "httpx>=0.28", "pytest>=8", "uvicorn[standard]>=0.40,<1"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
asyncio_default_fixture_loop_scope = "function"
